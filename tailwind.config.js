/** @type {import('tailwindcss').Config} */
module.exports = {
    // Enables class-based dark mode
    content: [
        "./src/**/*.{js,jsx,ts,tsx}", // Adjust for your project's structure
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};